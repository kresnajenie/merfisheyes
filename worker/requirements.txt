# Server-side ingestion worker — pinned for reproducible images.
# Single-cell path (process_spatial_data.py) needs numpy/pandas/scipy/anndata
# (+ h5py via anndata). pyarrow is for the single-molecule path (added ahead of
# wiring it). boto3 is used by the entrypoint for S3 download/upload.
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
anndata==0.10.9
# 3.12.1+ ships linux/arm64 wheels; 3.11 would compile from source (needs
# pkg-config + HDF5 headers) on Apple Silicon builds.
h5py==3.12.1
pyarrow==16.1.0
boto3==1.34.162
